[project]
name = "nvox-api"
version = "0.1.0"
description = "Nvox's API service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]==0.115.5",
    "uvicorn>=0.38.0",
    "nvox-db",
]
